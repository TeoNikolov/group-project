"apache-tomcat-8.5.11" is the web server needed to run Java code on the server-side.

"Webpage" is an example page which MUST be put into the '<apache directory>/webapps/ROOT/' directory. This directory is obtained after apache tomcat has been installed.
