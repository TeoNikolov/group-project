package compass;

public class HTMLFormatter {

    /* Function formats the given input (in accordance with the database),
    * and spits out an HTML to be used as a response. */
    public static String formatWeather(String jsonstr) {
        String result = "";

        return result;
    }

}
